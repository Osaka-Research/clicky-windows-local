<Window x:Class="ClickyWindows.ReplyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clicky"
        Width="460"
        SizeToContent="Height"
        MaxHeight="480"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Border Background="#EE101211"
            CornerRadius="10"
            BorderBrush="#332B63FF"
            BorderThickness="1"
            Padding="16,12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <TextBlock x:Name="ModeLabel" Text="Clicky" Foreground="#7C8AFF" FontFamily="Segoe UI" FontSize="12" FontWeight="Bold"/>
                <Button x:Name="CloseButton" Content="✕" HorizontalAlignment="Right"
                        Background="Transparent" BorderThickness="0" Foreground="#888888"
                        Padding="4,0" FontSize="12" Click="OnClose" Cursor="Hand"/>
            </Grid>

            <TextBlock x:Name="TranscriptText"
                       Grid.Row="1"
                       Foreground="#8A8F98"
                       FontFamily="Segoe UI"
                       FontSize="13"
                       FontStyle="Italic"
                       TextWrapping="Wrap"
                       Margin="0,0,0,8"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="420">
                <TextBlock x:Name="ReplyText"
                           Foreground="#ECEEED"
                           FontFamily="Segoe UI"
                           FontSize="19"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           LineHeight="27"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>

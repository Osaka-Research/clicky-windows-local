<Window x:Class="ClickyWindows.ReplyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clicky"
        Width="380"
        SizeToContent="Height"
        MaxHeight="420"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Border Background="#EE101211"
            CornerRadius="10"
            BorderBrush="#332B63FF"
            BorderThickness="1"
            Padding="16,12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.4" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="Clicky" Foreground="#7C8AFF" FontFamily="Segoe UI" FontSize="12" FontWeight="Bold"/>
                <Button x:Name="CloseButton" Content="✕" HorizontalAlignment="Right"
                        Background="Transparent" BorderThickness="0" Foreground="#888888"
                        Padding="4,0" FontSize="12" Click="OnClose" Cursor="Hand"/>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="380">
                <TextBlock x:Name="ReplyText"
                           Foreground="#ECEEED"
                           FontFamily="Segoe UI"
                           FontSize="14"
                           TextWrapping="Wrap"
                           LineHeight="20"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
